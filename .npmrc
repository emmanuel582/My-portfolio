# Include optional dependencies to avoid Rollup issues
include=optional
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Ensure proper platform-specific binaries
platform=linux
arch=x64
engine-strict=false 